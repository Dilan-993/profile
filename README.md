# Portfolio

## Урок №1. Создание структуры проекта

### Цель урока

На первом занятии мы не писали код сайта. Наша задача была научиться правильно организовывать проект, чтобы в дальнейшем в нем было легко ориентироваться.

Мы также познакомились с системой контроля версий Git и отправили проект в GitHub.

---

# Структура проекта

После первого урока проект имеет следующий вид:

```text
portfolio/
│
├── .gitignore
├── README.md
├── index.html
├── req.txt
│
└── assets/
    ├── css/
    ├── js/
    └── img/
```

Теперь разберем назначение каждого файла и каждой папки.

---

## index.html

Это **главная страница сайта**.

Именно ее открывает браузер.

Пока файл пустой, но именно здесь будет находиться вся структура нашего сайта:

* заголовки;
* текст;
* изображения;
* кнопки;
* ссылки;
* блоки страницы.

Можно сказать, что **HTML — это каркас будущего дома**.

---

## assets

Название **assets** переводится как:

* ресурсы;
* материалы;
* дополнительные файлы проекта.

Эта папка нужна для хранения файлов, которые используются сайтом.

Мы сразу создали такую структуру, чтобы привыкать к правильной организации проекта.

---

### css

В этой папке будут находиться файлы стилей.

Именно CSS отвечает за внешний вид сайта:

* цвета;
* размеры;
* шрифты;
* расположение элементов;
* анимации.

---

### js

В этой папке будут храниться JavaScript-файлы.

JavaScript делает сайт "живым".

Например:

* обработка нажатий кнопок;
* анимации;
* появление меню;
* отправка данных без перезагрузки страницы.

---

### img

В этой папке будут храниться изображения сайта.

Например:

* фотографии;
* логотип;
* иконки;
* фоновые картинки.

---

## README.md

Это файл с описанием проекта.

Обычно здесь размещают:

* информацию о проекте;
* инструкцию по запуску;
* список возможностей.

В нашем проекте README.md станет **конспектом курса**, который будет пополняться после каждого урока.

---

## .gitignore

Git отслеживает изменения файлов.

Но некоторые файлы хранить в репозитории не нужно.

Для этого существует файл `.gitignore`.

В нем перечисляются файлы и папки, которые Git должен игнорировать.

Сейчас в нем находится:

```text
.idea/
.venv
__pycache__/

```

---

## req.txt

Пока этот файл пустой.

В дальнейшем здесь будет список библиотек Python, необходимых для работы backend-части проекта.

Например:

```text
fastapi
uvicorn
jinja2
и др.
```

Позже мы узнаем, зачем нужен этот файл и как Python устанавливает зависимости из него.

---

# Первое знакомство с Git

Git — это система контроля версий.

Она помогает сохранять историю проекта.

Можно посмотреть:

* какие изменения были сделаны;
* когда они были сделаны;
* кем они были сделаны.

Git можно сравнить с сохранениями в компьютерной игре.

После каждого важного этапа мы будем делать "сохранение" проекта.

Такое сохранение называется **коммитом**.

---

# Команды Git

## git init

```bash
git init
```

Создает Git-репозиторий внутри проекта.

После выполнения этой команды Git начинает отслеживать изменения файлов.

Эта команда выполняется только один раз — при создании проекта.

---

## git remote add origin

```bash
git remote add origin <ссылка_на_репозиторий>
```

Подключает локальный проект к удаленному репозиторию GitHub.

Проще говоря:

* проект находится на компьютере;
* GitHub находится в интернете;
* эта команда "знакомит" их между собой.

---

## git add

```bash
git add .
```

Добавляет все измененные файлы в список файлов для следующего коммита.

Можно представить это как корзину перед сохранением.

Мы говорим Git:

> "Вот эти изменения нужно сохранить."

---

## git commit

```bash
git commit -m "Организовали структуру проекта"
```

Создает новую точку сохранения проекта.

Текст после `-m` называется **сообщением коммита**.

По нему можно понять, что именно изменилось.

---

## git push

```bash
git push -u origin master
```

Отправляет все коммиты на GitHub.

После этого изменения появляются в удаленном репозитории.

> **Примечание.**
> Во многих современных проектах вместо ветки `master` используется ветка `main`. Если репозиторий был создан с веткой `main`, то команда будет выглядеть так:
>
> ```bash
> git push -u origin main
> ```

---

# Первый коммит

После завершения первого урока мы сделали первый коммит:

```text
Организовали структуру проекта
```

Теперь история нашего проекта официально началась.

После каждого нового урока мы будем делать новый коммит, чтобы можно было посмотреть, как постепенно развивается сайт.

---

# Мы научились

После первого занятия мы научились:

* создавать структуру проекта;
* понимать назначение каждой папки;
* создавать Git-репозиторий;
* подключать проект к GitHub;
* делать первый коммит;
* отправлять изменения в удаленный репозиторий.

Это фундамент, на котором будет строиться весь дальнейший проект. 


# Урок №2. Создание первой HTML-страницы

## Цель урока

На этом занятии мы познакомились с языком разметки **HTML**.

Мы узнали, из каких частей состоит HTML-документ, научились создавать первые элементы страницы и поняли, зачем нужны основные теги.

В течение урока мы работали **только с файлом `index.html`**.

---

# Что такое HTML?

**HTML (HyperText Markup Language)** — это язык разметки.

Он нужен для создания структуры веб-страницы.

Если сравнить сайт с домом, то HTML — это его каркас.

Именно HTML сообщает браузеру:

* где находится заголовок;
* где будет текст;
* где расположено меню;
* где появится изображение;
* где будут ссылки и кнопки.

Сам по себе HTML не делает страницу красивой.

За внешний вид отвечает CSS, а за поведение сайта — JavaScript.

---

# Структура HTML-документа

Каждая HTML-страница начинается с объявления типа документа.

```html
<!DOCTYPE html>
```

Эта строка сообщает браузеру, что перед ним современный HTML-документ.

---

После этого начинается сам документ.

```html
<html lang="ru">
```

Тег `<html>` является главным тегом всей страницы.

Внутри него находится абсолютно всё содержимое сайта.

Атрибут `lang="ru"` сообщает браузеру, что страница написана на русском языке.

Это помогает:

* браузеру;
* поисковым системам;
* программам для чтения текста.

---

# Тег `<head>`

Внутри `<head>` хранится служебная информация.

Она почти никогда не отображается на самой странице.

В нашем проекте находятся:

```html
<head>

    <meta charset="UTF-8">

    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Вася Пупкин * начинающий программист</title>

</head>
```

Разберем каждый элемент.

---

## `<meta charset="UTF-8">`

Указывает кодировку страницы.

Благодаря этому русский текст отображается правильно.

Если забыть эту строку, вместо букв могут появиться непонятные символы.

---

## `<meta name="viewport"...>`

Этот тег нужен для правильного отображения сайта на телефонах и планшетах.

Без него сайт может выглядеть слишком маленьким или неудобным на мобильных устройствах.

Практически все современные сайты используют этот тег.

---

## `<title>`

Определяет название страницы.

Этот текст отображается:

* на вкладке браузера;
* в закладках;
* в результатах поисковых систем.

В нашем проекте:

```html
<title>Гриша Галатов * начинающий программист</title>
```

---

# Тег `<body>`

Внутри `<body>` располагается всё, что видит пользователь.

Именно здесь создается содержимое сайта.

Все тексты, изображения, кнопки и меню находятся внутри этого тега.

---

# Шапка сайта — `<header>`

Мы создали первый блок страницы.

```html
<header>
```

Обычно здесь размещают:

* логотип;
* название сайта;
* меню навигации;
* кнопки входа или регистрации.

Практически каждый современный сайт начинается именно с шапки.

---

# Логотип

Мы использовали тег

```html
<div class="logo">
```

Пока внутри находится только текст.

Позже вместо него можно разместить:

* изображение;
* настоящий логотип;
* имя владельца сайта.

---

# Навигация — `<nav>`

Тег

```html
<nav>
```

используется для создания меню сайта.

Внутри него находятся ссылки на различные разделы страницы.

---

# Ссылки — `<a>`

Мы познакомились с тегом ссылки.

Например:

```html
<a href="#about">Обо мне</a>
```

Текст между тегами отображается на странице.

А атрибут `href` указывает, куда должна вести ссылка.

Сейчас ссылки содержат значения:

```text
#about
#skills
#contacts
```

Пока эти разделы еще не созданы.

Позже мы их добавим, и ссылки начнут автоматически прокручивать страницу к нужному месту.

---

# Первый основной блок страницы

После шапки мы начали создавать содержимое сайта.

Для этого использовали блок

```html
<section class="hero">
```

Такой блок обычно называют **Hero Section**.

Это первая большая часть страницы, которую пользователь видит сразу после открытия сайта.

Именно здесь чаще всего располагаются:

* приветствие;
* имя;
* профессия;
* фотография;
* кнопка действия.

---

# Заголовки

Мы познакомились с тегами заголовков.

```html
<h1>Добрый день 👋</h1>
```

`<h1>` — самый главный заголовок страницы.

На странице рекомендуется использовать только один такой заголовок.

---

```html
<h2>Я Гриша Галатов</h2>
```

`<h2>` — заголовок второго уровня.

Он используется для подзаголовков.

---

# Абзац

Для обычного текста используется тег

```html
<p>
```

Внутри него мы написали небольшую информацию о себе.

---

# Кнопка

Мы создали ссылку

```html
<a class="button" href="#contacts">
    Связаться
</a>
```

Пока это обычная ссылка.

Позже с помощью CSS она станет красивой кнопкой.

---

# Атрибут class

Во время урока мы несколько раз использовали атрибут

```html
class=""
```

Например:

```html
class="logo"
class="hero"
class="hero-text"
class="button"
```

Сам по себе `class` ничего не делает.

Он служит именем элемента.

Позже CSS сможет найти этот элемент по имени и изменить его внешний вид.

---

# Что получилось в итоге

После второго урока мы создали основу будущего сайта:

* шапку;
* логотип;
* меню;
* приветственный блок;
* заголовки;
* описание;
* кнопку связи.

Пока страница выглядит очень просто. 


---

# Итоги урока

После второго занятия мы научились:

* понимать структуру HTML-документа;
* создавать основные HTML-теги;
* использовать заголовки;
* создавать ссылки;
* писать обычный текст;
* создавать логические блоки страницы;
* использовать атрибут `class`;
* понимать назначение тегов `header`, `nav` и `section`.


# Урок №3. Создание основных разделов сайта

## Цель урока

На этом занятии мы продолжили работу с файлом **`index.html`**.

Мы научились создавать новые разделы сайта с помощью тега `<section>`, познакомились с тегом `<footer>` и практически завершили структуру нашей страницы.

После этого урока сайт уже состоит из нескольких самостоятельных частей, между которыми можно переходить с помощью меню.

---

# Повторение

На прошлом уроке мы познакомились с:

* тегом `<header>`;
* меню навигации `<nav>`;
* ссылками `<a>`;
* главным блоком страницы `<section class="hero">`.

Сегодня мы продолжили создавать структуру сайта.

---

# Тег `<section>`

Во время урока мы несколько раз использовали тег

```html
<section>
```

`<section>` переводится как **раздел**.

Этот тег помогает разделить страницу на логические части.

Можно представить страницу сайта как книгу.

Тогда:

* каждая глава книги — это `<section>`;
* вся книга — это веб-страница.

Такой подход делает код более понятным и удобным для чтения.

---

# Раздел "Обо мне"

Мы создали первый информационный раздел сайта.

```html
<section id="about">

    <h2>Обо мне</h2>

    <p>
        Мне 14 лет...
    </p>

</section>
```

В этом разделе пользователь может познакомиться с владельцем сайта.

Мы использовали:

* `<section>` — новый раздел страницы;
* `<h2>` — заголовок раздела;
* `<p>` — текст с информацией.

---

# Атрибут `id`

Во время урока появился новый атрибут:

```html
id="about"
```

Раньше мы уже использовали атрибут `class`.

Теперь познакомились еще с одним важным атрибутом — `id`.

Главное отличие:

* `class` можно использовать много раз;
* `id` должен быть уникальным.

На одной странице не должно быть двух одинаковых `id`.

---

# Зачем нужен `id`?

В начале страницы мы уже создали меню.

Например:

```html
<a href="#about">Обо мне</a>
```

Здесь значение `href="#about"` совпадает со значением

```html
id="about"
```

Когда пользователь нажимает на ссылку, браузер автоматически находит элемент с таким `id` и переходит к нему.

Именно поэтому меню работает без использования JavaScript.

---

# Раздел "Навыки"

Следующим мы создали раздел навыков.

```html
<section id="skills">
```

Внутри раздела находится заголовок и список навыков.

---

# Контейнер `<div class="cards">`

Мы создали специальный блок

```html
<div class="cards">
```

Пока он никак не изменяет внешний вид страницы.

Но позже CSS превратит его в красивый контейнер для карточек.

---

# Карточки навыков

Каждый навык помещен в отдельный блок.

Например:

```html
<div class="card">

    python

</div>
```

Каждая такая карточка сейчас содержит только текст.

На следующих уроках мы научимся делать красивые карточки с помощью CSS.

---

# Почему используются одинаковые `class`?

Мы создали несколько элементов

```html
class="card"
```

Это сделано специально.

Все карточки должны выглядеть одинаково.

Когда мы начнем изучать CSS, достаточно будет написать стиль только один раз.

Он автоматически применится ко всем карточкам.

Это одна из главных особенностей использования `class`.

---

# Раздел "Контакты"

Следующий раздел посвящен контактной информации.

```html
<section id="contacts">
```

Внутри него находится блок

```html
<div class="socials">
```

В будущем он поможет красиво расположить ссылки.

---

# Ссылка на VK

Мы создали первую внешнюю ссылку.

```html
<a href="https://vk.com/..." target="_blank">

    VK

</a>
```

Появился новый атрибут

```html
target="_blank"
```

Он говорит браузеру:

> "Открой ссылку в новой вкладке."

Благодаря этому пользователь не покидает наш сайт.

---

# Ссылка на электронную почту

Мы также познакомились с новым способом создания ссылки.

```html
<a href="mailto:grysha1021@gmail.com">

    email

</a>
```

Перед адресом используется слово

```text
mailto:
```

Такая ссылка не открывает сайт.

Она предлагает открыть почтовую программу для отправки письма.

---

# Тег `<footer>`

Последним элементом страницы стал

```html
<footer>
```

`footer` переводится как **подвал сайта**.

Это нижняя часть страницы.

Чаще всего здесь размещают:

* имя автора;
* контактную информацию;
* ссылки;
* информацию об авторских правах;
* год создания сайта.

В нашем проекте мы написали:

```html
<footer>

    сайт разработан Гришей Галатовым

</footer>
```

Теперь страница имеет логическое завершение.

---

# Структура сайта после третьего урока

Теперь сайт состоит из следующих частей:

```text
Header
│
├── Hero
├── Обо мне
├── Навыки
├── Контакты
└── Footer
```

Практически любой современный сайт имеет похожую структуру.

---

# Что получилось

После третьего урока наша страница уже содержит:

* меню;
* приветственный экран;
* информацию о владельце;
* список навыков;
* контакты;
* нижнюю часть сайта.

Пока сайт выглядит очень просто, потому что мы еще не подключили CSS.

Однако структура страницы уже полностью готова.

---

# Итоги урока

После третьего занятия мы научились:

* создавать новые разделы сайта с помощью `<section>`;
* использовать атрибут `id`;
* понимать разницу между `id` и `class`;
* связывать ссылки меню с разделами страницы;
* создавать списки карточек;
* создавать ссылки на внешние сайты;
* создавать ссылки на электронную почту;
* использовать тег `<footer>`;
* понимать общую структуру современного сайта.

Теперь наш сайт имеет практически готовую HTML-структуру. На следующих уроках мы начнем изучать CSS и научимся превращать обычный текст в красивую современную веб-страницу.


# Урок №4. Первое знакомство с CSS

## Цель урока

На этом занятии мы познакомились с языком **CSS** и впервые начали изменять внешний вид нашего сайта.

До этого момента HTML отвечал только за структуру страницы.

Теперь мы узнали, как сделать страницу красивой с помощью CSS.

---

# Что такое CSS?

**CSS (Cascading Style Sheets)** — это язык стилей.

Если HTML — это каркас дома, то CSS — это его отделка.

Именно CSS отвечает за:

* цвета;
* размеры текста;
* расположение элементов;
* шрифты;
* отступы;
* анимации.

Без CSS практически все сайты выглядели бы как обычный черный текст на белом фоне.

---

# Как подключить CSS?

Мы создали файл

```text
assets/css/style.css
```

После этого подключили его в файле `index.html`.

```html
<link rel="stylesheet" href="assets/css/style.css">
```

Теперь браузер знает, где находятся стили нашего сайта.

Каждый раз при открытии страницы браузер сначала загружает HTML, а затем применяет к нему правила из CSS.

---

# Как устроено правило CSS?

Практически каждое правило CSS выглядит одинаково.

```css
селектор {

    свойство: значение;

}
```

Например:

```css
body {

    background: #0d1117;

}
```

Здесь:

* `body` — селектор;
* `background` — свойство;
* `#0d1117` — значение.

Можно сказать так:

> "Найди элемент `<body>` и сделай его фон темным."

---

# Первый селектор — `*`

Первым правилом нашего файла стало:

```css
* {

    margin: 0;
    padding: 0;
    box-sizing: border-box;

}
```

Знак

```css
*
```

называется **универсальным селектором**.

Он выбирает **все элементы страницы**.

Это значит, что правило применяется сразу ко всему сайту.

---

# Свойство `margin`

```css
margin: 0;
```

`margin` — это внешний отступ элемента.

По умолчанию браузер сам добавляет небольшие отступы.

Из-за этого разные браузеры могут отображать страницу немного по-разному.

Мы убрали эти отступы, чтобы полностью управлять расположением элементов самостоятельно.

---

# Свойство `padding`

```css
padding: 0;
```

`padding` — это внутренний отступ.

Он определяет расстояние между содержимым элемента и его границами.

Пока мы также обнулили его.

Позже мы научимся использовать `padding`, чтобы красиво располагать текст внутри блоков.

---

# Свойство `box-sizing`

```css
box-sizing: border-box;
```

Это одно из самых полезных свойств CSS.

Оно изменяет способ вычисления размеров элементов.

Благодаря ему становится намного проще рассчитывать ширину и высоту блоков.

Практически во всех современных проектах используется именно

```css
box-sizing: border-box;
```

---

# Стили для `<body>`

Следующее правило:

```css
body {

    background: #0d1117;

    color: #ffffff;

}
```

Теперь мы изменили внешний вид всей страницы.

---

# Свойство `background`

```css
background: #0d1117;
```

`background` отвечает за фон элемента.

Мы сделали фон страницы темным.

---

# Что такое HEX-цвет?

В CSS существует несколько способов записи цветов.

Мы использовали запись

```css
#0d1117
```

Такой способ называется **HEX-кодом**.

Каждый цвет имеет собственный код.

Например:

```text
Белый      → #ffffff

Черный     → #000000

Красный    → #ff0000

Зеленый    → #00ff00

Синий      → #0000ff
```

Позже мы познакомимся и с другими способами задания цветов.

---

# Свойство `color`

```css
color: #ffffff;
```

Свойство `color` изменяет цвет текста.

После этого правила весь текст страницы стал белым.

Теперь белые буквы хорошо читаются на темном фоне.

---

# Что получилось?

После сегодняшнего урока сайт изменился совсем немного.

Но произошло очень важное событие:

Мы впервые отделили **структуру** страницы от ее **оформления**.

Теперь:

* HTML отвечает за содержимое сайта.
* CSS отвечает за внешний вид сайта.

Именно так создаются практически все современные веб-приложения.

---

# Итоги урока

После четвертого занятия мы научились:

* понимать назначение CSS;
* подключать файл стилей к HTML;
* понимать устройство CSS-правила;
* использовать универсальный селектор `*`;
* работать со свойствами `margin`, `padding` и `box-sizing`;
* изменять фон страницы;
* изменять цвет текста;
* познакомились с HEX-кодами цветов.

Теперь мы сделали первый шаг к созданию красивого современного сайта.


# Урок №5. Стилизация шапки сайта и меню навигации

## Цель урока

На этом занятии мы продолжили изучать CSS.

Мы научились изменять внешний вид шапки сайта, познакомились с технологией **Flexbox**, оформили меню навигации и впервые сделали элементы страницы интерактивными.

После этого урока шапка сайта стала выглядеть как на современных веб-сайтах.

---

# Повторение

На прошлом уроке мы:

* подключили файл `style.css`;
* познакомились с CSS;
* изменили фон страницы;
* изменили цвет текста.

Сегодня мы будем работать с элементами, которые создали в файле `index.html`:

```html
<header>

    <div class="logo">

        dilan993

    </div>

    <nav>

        <a href="#about">Обо мне</a>

        <a href="#skills">Навыки</a>

        <a href="#contacts">Связь со мной</a>

    </nav>

</header>
```

---

# Стили для `<header>`

Мы написали первое правило для шапки сайта.

```css
header{

    display:flex;

    justify-content:space-between;

    padding:25px 10%;

    position:fixed;

    width:100%;

    background:#161b22;

}
```

Разберем каждое свойство отдельно.

---

# Свойство `display`

```css
display:flex;
```

До этого момента браузер располагал элементы друг под другом.

Свойство

```css
display:flex;
```

включает специальный режим расположения элементов — **Flexbox**.

Flexbox помогает удобно располагать элементы по горизонтали или вертикали.

После включения Flexbox логотип и меню оказались на одной линии.

---

# Что такое Flexbox?

Представьте коробку.

Внутри нее лежат несколько предметов.

Flexbox позволяет легко управлять расположением этих предметов.

Например:

* поставить рядом;
* раздвинуть;
* выровнять по центру;
* расположить справа или слева.

Сегодня мы познакомились с Flexbox впервые.

В следующих уроках мы будем использовать его очень часто.

---

# Свойство `justify-content`

```css
justify-content:space-between;
```

Это свойство управляет расположением элементов по горизонтали.

Значение

```css
space-between
```

раздвигает элементы максимально далеко друг от друга.

В нашем случае получилось так:

```text
Логотип                               Меню
```

Логотип оказался слева, а меню — справа.

---

# Свойство `padding`

```css
padding:25px 10%;
```

Мы уже знаем, что `padding` отвечает за внутренние отступы.

Здесь указаны сразу два значения.

Первое число

```text
25px
```

означает верхний и нижний отступ.

Второе значение

```text
10%
```

означает отступы слева и справа.

Благодаря этому содержимое не "прилипает" к краям страницы.

---

# Свойство `position`

```css
position:fixed;
```

Это свойство изменяет способ расположения элемента.

Значение

```text
fixed
```

закрепляет элемент на экране.

Теперь шапка сайта остается сверху даже тогда, когда пользователь прокручивает страницу вниз.

Такое решение очень часто используется на современных сайтах.

---

# Свойство `width`

```css
width:100%;
```

Ширина шапки стала равна всей ширине окна браузера.

Теперь `<header>` занимает всю верхнюю часть страницы.

---

# Свойство `background`

```css
background:#161b22;
```

Мы изменили цвет фона шапки.

Теперь она отличается от основного фона страницы.

Так меню стало более заметным.

---

# Стили для меню

Следующее правило относится к тегу `<nav>`.

```css
nav{

    display:flex;

    gap:30px;

}
```

---

# Свойство `gap`

```css
gap:30px;
```

`gap` отвечает за расстояние между элементами.

Теперь между ссылками появилось одинаковое пространство.

Без этого свойства все ссылки располагались бы вплотную друг к другу.

---

# Стили для ссылок

Мы изменили внешний вид ссылок меню.

```css
nav a{

    text-decoration:none;

    color:white;

    transition:.3s;

}
```

---

# Свойство `text-decoration`

```css
text-decoration:none;
```

По умолчанию браузер подчеркивает ссылки.

Это свойство убирает подчеркивание.

Теперь ссылки выглядят аккуратнее.

---

# Свойство `color`

```css
color:white;
```

Изменяет цвет текста.

Мы сделали ссылки белыми.

---

# Свойство `transition`

```css
transition:.3s;
```

Появилось новое свойство.

Оно отвечает за плавность изменения.

Если изменить цвет без `transition`, это произойдет мгновенно.

С `transition` изменение занимает 0.3 секунды и выглядит намного красивее.

---

# Псевдокласс `:hover`

Последнее правило сегодняшнего урока:

```css
nav a:hover{

    color:#58a6ff;

}
```

Псевдокласс

```text
:hover
```

срабатывает тогда, когда пользователь наводит курсор мыши на элемент.

В нашем случае ссылка меняет цвет.

Так пользователь понимает, что именно на эту ссылку сейчас наведен курсор.

Практически все современные сайты используют подобный эффект.

---

# Что получилось?

После сегодняшнего урока сайт изменился очень заметно.

Теперь:

* логотип расположен слева;
* меню находится справа;
* ссылки аккуратно разделены;
* шапка закреплена в верхней части страницы;
* ссылки реагируют на наведение мыши.

Сайт стал выглядеть гораздо современнее.

---

# Итоги урока

После пятого занятия мы научились:

* использовать Flexbox;
* располагать элементы по горизонтали;
* использовать свойство `justify-content`;
* создавать внутренние отступы;
* закреплять элементы с помощью `position: fixed`;
* задавать ширину элементов;
* изменять внешний вид ссылок;
* использовать свойство `gap`;
* делать плавные анимации с помощью `transition`;
* создавать эффекты наведения с помощью `:hover`.

Теперь шапка нашего сайта выглядит как у большинства современных веб-сайтов.


# Урок №6. Оформление главного экрана сайта

## Цель урока

На этом занятии мы продолжили оформление нашего сайта.

Мы изменили внешний вид главного экрана, познакомились с новыми свойствами CSS и впервые превратили обычную ссылку в красивую кнопку.

После этого урока сайт стал намного больше похож на современную страницу-визитку.

---

# Повторение

На прошлом занятии мы:

* оформили шапку сайта;
* познакомились с Flexbox;
* сделали горизонтальное меню;
* научились изменять цвет ссылок при наведении мыши.

Теперь перейдем к главному экрану сайта.

---

# Стили для блока `.hero`

Мы начали с оформления блока приветствия.

```css
.hero{

    height:100vh;

    display:flex;

    align-items:center;

    padding:0 10%;

}
```

Этот блок пользователь видит сразу после открытия сайта.

Поэтому его называют **Hero Section** — главный экран страницы.

---

# Свойство `height`

```css
height:100vh;
```

`height` задает высоту элемента.

Мы использовали новое значение

```text
100vh
```

`vh` означает **высоту окна браузера**.

Запись

```text
100vh
```

говорит браузеру:

> "Сделай этот блок высотой во весь экран."

Поэтому главный экран занимает всю видимую область страницы.

---

# Свойство `display`

```css
display:flex;
```

Мы уже знакомы с Flexbox.

Снова используем его, чтобы удобно расположить содержимое блока.

---

# Свойство `align-items`

```css
align-items:center;
```

Это новое свойство Flexbox.

Оно отвечает за расположение элементов по вертикали.

Значение

```text
center
```

размещает содержимое ровно по центру.

Теперь текст находится не сверху страницы, а красиво расположен по середине экрана.

---

# Свойство `padding`

```css
padding:0 10%;
```

Мы снова использовали внутренние отступы.

Сверху и снизу отступ равен нулю.

Слева и справа содержимое отодвигается от краев окна на 10%.

Благодаря этому текст становится удобнее читать.

---

# Заголовок первого уровня

```css
.hero h1{

    font-size:64px;

}
```

Мы увеличили размер главного заголовка.

---

# Свойство `font-size`

```css
font-size:64px;
```

`font-size` отвечает за размер текста.

Теперь главный заголовок стал большим и хорошо заметным.

Это первое, что видит посетитель сайта.

---

# Подзаголовок

Следующее правило:

```css
.hero h2{

    margin:20px 0;

    font-size:36px;

    color:#58a6ff;

}
```

---

# Свойство `margin`

```css
margin:20px 0;
```

Мы уже знаем, что `margin` отвечает за внешние отступы.

Здесь используются два значения.

Первое

```text
20px
```

создает расстояние сверху и снизу.

Второе

```text
0
```

означает отсутствие отступов слева и справа.

Так между заголовками появилось свободное пространство.

---

# Цвет подзаголовка

```css
color:#58a6ff;
```

Мы сделали подзаголовок голубым.

Это помогает выделить имя владельца сайта.

---

# Стили для текста

```css
.hero p{

    width:600px;

    line-height:1.8;

}
```

---

# Свойство `width`

```css
width:600px;
```

Теперь ширина текста ограничена.

Если не ограничивать ширину, длинные строки становится трудно читать.

Именно поэтому разработчики часто задают максимальную ширину текстовых блоков.

---

# Свойство `line-height`

```css
line-height:1.8;
```

Это расстояние между строками текста.

Чем больше значение, тем легче читать длинные предложения.

Поэтому текст стал выглядеть более аккуратно.

---

# Создание кнопки

В HTML у нас была обычная ссылка.

```html
<a class="button" href="#contacts">

    Связаться

</a>
```

Теперь с помощью CSS мы превратили ее в кнопку.

---

# Стили `.button`

```css
.button{

    display:inline-block;

    margin-top:40px;

    padding:15px 40px;

    background:#238636;

    color:white;

    text-decoration:none;

    border-radius:10px;

    transition:.3s;

}
```

Разберем новые свойства.

---

# Свойство `display:inline-block`

По умолчанию ссылка занимает только место, необходимое для текста.

Свойство

```css
display:inline-block;
```

позволяет задать ссылке внутренние отступы и сделать ее похожей на настоящую кнопку.

---

# Свойство `margin-top`

```css
margin-top:40px;
```

Добавляет внешний отступ сверху.

Теперь кнопка располагается немного ниже текста.

Так страница выглядит аккуратнее.

---

# Свойство `background`

```css
background:#238636;
```

Мы сделали фон кнопки зеленым.

Теперь она хорошо выделяется на странице.

---

# Свойство `text-decoration`

```css
text-decoration:none;
```

Убирает стандартное подчеркивание ссылки.

После этого элемент становится похож на настоящую кнопку.

---

# Свойство `border-radius`

```css
border-radius:10px;
```

Закругляет углы элемента.

Теперь кнопка выглядит современно.

Практически все современные сайты используют небольшое закругление углов.

---

# Свойство `transition`

```css
transition:.3s;
```

Мы уже использовали это свойство в меню.

Оно делает изменения плавными.

---

# Эффект наведения

Последнее правило:

```css
.button:hover{

    transform:translateY(-3px);

}
```

Когда пользователь наводит курсор на кнопку, она немного поднимается вверх.

Такой небольшой эффект делает сайт более "живым".

---

# Свойство `transform`

```css
transform:translateY(-3px);
```

`transform` позволяет изменять положение, размер или поворот элементов.

Сегодня мы познакомились только с одной возможностью.

Значение

```text
translateY(-3px)
```

поднимает кнопку вверх на 3 пикселя.

Позже мы подробнее изучим возможности свойства `transform`.

---

# Что получилось?

После сегодняшнего урока главный экран сайта полностью преобразился.

Теперь:

* заголовок стал большим;
* подзаголовок получил собственный цвет;
* текст стал удобнее читать;
* главный экран занимает всю высоту окна браузера;
* ссылка превратилась в красивую кнопку;
* кнопка получила эффект наведения.

Сайт стал выглядеть гораздо современнее и привлекательнее.

---

# Итоги урока

После шестого занятия мы научились:

* задавать высоту элементов;
* использовать единицу измерения `vh`;
* выравнивать элементы по вертикали;
* изменять размер текста;
* создавать внешние и внутренние отступы;
* ограничивать ширину текстовых блоков;
* изменять расстояние между строками;
* превращать ссылки в кнопки;
* закруглять углы элементов;
* использовать простую анимацию при наведении курсора.

Теперь главный экран нашего сайта выглядит как у настоящего сайта-визитки и готов встречать посетителей.


# Урок №7. Создание карточек и оформление разделов сайта

## Цель урока

На этом занятии мы продолжили оформление сайта с помощью CSS.

Мы научились создавать красивые разделы страницы, познакомились с технологией **Grid**, оформили карточки навыков и сделали ссылки на контакты более привлекательными.

После этого урока сайт стал выглядеть намного ближе к современным веб-сайтам.

---

# Повторение

На прошлых занятиях мы:

* оформили шапку сайта;
* познакомились с Flexbox;
* создали главный экран;
* превратили ссылку в красивую кнопку.

Теперь займемся оформлением остальных разделов сайта.

---

# Стили для всех разделов

Первое правило:

```css
section{

    padding:120px 10%;

}
```

---

# Свойство `padding`

Мы уже знакомы с этим свойством.

Теперь оно применяется сразу ко всем тегам `<section>`.

```css
padding:120px 10%;
```

Это означает:

* сверху и снизу — отступ 120 пикселей;
* слева и справа — 10% ширины страницы.

Благодаря этому разделы перестали "слипаться" друг с другом.

Страница стала выглядеть более аккуратно.

---

# Контейнер карточек

Следующее правило относится к блоку

```css
.cards
```

Полностью оно выглядит так:

```css
.cards{

    display:grid;

    grid-template-columns:repeat(auto-fit,minmax(170px,1fr));

    gap:20px;

    margin-top:40px;

}
```

---

# Что такое Grid?

До этого момента мы использовали **Flexbox**.

Flexbox отлично подходит для расположения элементов в одну строку или один столбец.

Но если элементов становится много, гораздо удобнее использовать **Grid**.

Grid позволяет строить настоящую сетку из элементов.

Например:

```text
┌────────┐ ┌────────┐ ┌────────┐

│ Card 1 │ │ Card 2 │ │ Card 3 │

└────────┘ └────────┘ └────────┘

┌────────┐ ┌────────┐

│ Card 4 │ │ Card 5 │

└────────┘ └────────┘
```

Именно такую сетку мы создаем для наших навыков.

Сегодня мы познакомились с Grid впервые.

Позже мы посвятим этой технологии отдельный урок.

---

# Свойство `display:grid`

```css
display:grid;
```

Оно включает режим сетки.

Теперь браузер самостоятельно располагает карточки красивыми рядами.

---

# Свойство `grid-template-columns`

```css
grid-template-columns:
repeat(auto-fit,minmax(170px,1fr));
```

Это самое сложное правило сегодняшнего урока.

Пока достаточно понять только основную идею.

Мы говорим браузеру:

> "Создай столько колонок, сколько помещается на экране, но каждая карточка должна иметь удобную ширину."

Благодаря этому сайт автоматически подстраивается под размер окна браузера.

Позже мы подробно изучим каждую часть этой конструкции.

---

# Свойство `gap`

```css
gap:20px;
```

Мы уже использовали `gap` при создании меню.

Теперь он добавляет расстояние между карточками.

Все карточки располагаются на одинаковом расстоянии друг от друга.

---

# Свойство `margin-top`

```css
margin-top:40px;
```

Добавляет свободное пространство между заголовком раздела и карточками.

Так информация выглядит более читаемой.

---

# Стили карточек

Каждая карточка получила собственное оформление.

```css
.card{

    background:#161b22;

    padding:40px;

    text-align:center;

    border-radius:15px;

    transition:.3s;

}
```

---

# Свойство `background`

Мы изменили фон карточек.

Теперь каждая карточка выделяется на фоне страницы.

---

# Свойство `padding`

```css
padding:40px;
```

Добавляет внутренние отступы.

Теперь текст не касается краев карточки.

---

# Свойство `text-align`

```css
text-align:center;
```

Выравнивает текст по центру.

Название каждого навыка теперь располагается ровно посередине карточки.

---

# Свойство `border-radius`

```css
border-radius:15px;
```

Закругляет углы карточек.

Так они выглядят современно и аккуратно.

---

# Свойство `transition`

```css
transition:.3s;
```

Мы уже знакомы с этим свойством.

Оно делает изменения плавными.

---

# Эффект наведения

Следующее правило:

```css
.card:hover{

    transform:translateY(-8px);

    background:#21262d;

}
```

Когда пользователь наводит курсор на карточку, происходят два изменения.

---

# Карточка поднимается вверх

```css
transform:translateY(-8px);
```

Карточка немного приподнимается.

Так пользователь сразу понимает, что элемент активный.

---

# Изменение цвета

```css
background:#21262d;
```

Во время наведения карточка становится немного светлее.

Так эффект выглядит еще красивее.

---

# Блок контактов

Теперь перейдем к контактам.

```css
.socials{

    display:flex;

    gap:25px;

    margin-top:40px;

}
```

---

# Flexbox снова

Мы снова используем

```css
display:flex;
```

Теперь ссылки располагаются по горизонтали.

---

# Расстояние между ссылками

```css
gap:25px;
```

Добавляет одинаковое расстояние между кнопками.

---

# Отступ сверху

```css
margin-top:40px;
```

Контакты немного отодвигаются от заголовка раздела.

---

# Стили ссылок

Последнее правило:

```css
.socials a{

    text-decoration:none;

    padding:15px 30px;

    background:#238636;

    color:white;

    border-radius:8px;

}
```

Теперь ссылки стали похожи на небольшие кнопки.

Мы уже знакомы почти со всеми этими свойствами:

* `text-decoration` убирает подчеркивание;
* `padding` делает кнопку больше;
* `background` задает цвет;
* `color` изменяет цвет текста;
* `border-radius` закругляет углы.

Получается, что мы не изучили много новых свойств, а научились использовать уже знакомые инструменты в разных частях сайта.

---

# Что получилось?

После сегодняшнего урока сайт заметно преобразился.

Теперь:

* разделы получили красивые отступы;
* навыки отображаются в виде карточек;
* карточки реагируют на наведение мыши;
* ссылки на контакты стали похожи на кнопки;
* страница выглядит более современной и аккуратной.

---

# Итоги урока

После седьмого занятия мы научились:

* оформлять разделы сайта;
* познакомились с технологией Grid;
* создавать сетку из карточек;
* использовать свойство `text-align`;
* оформлять карточки навыков;
* изменять внешний вид карточек при наведении;
* использовать уже знакомые свойства CSS для оформления новых элементов;
* создавать красивые кнопки для раздела контактов.

Теперь практически все основные разделы нашего сайта имеют современный внешний вид и готовы к завершению оформления.


# Урок №8. Завершение оформления сайта и добавление фотографии

## Цель урока

На этом занятии мы завершили оформление сайта с помощью CSS.

Мы научились красиво размещать фотографию рядом с текстом, познакомились с новыми свойствами Flexbox, научились работать с изображениями и оформили нижнюю часть страницы.

После этого урока наш сайт стал выглядеть как настоящее портфолио.

---

# Повторение

На предыдущих занятиях мы уже оформили:

* шапку сайта;
* главный экран;
* раздел навыков;
* карточки;
* контакты.

Теперь осталось оформить раздел **«Обо мне»** и нижнюю часть страницы.

---

# Новая структура HTML

Мы изменили раздел "Обо мне".

Теперь он состоит из двух частей:

```html
<div class="about-container">

    <div class="about-photo">

        <img src="assets/img/me.jpg" alt="Моя фотография">

    </div>

    <div class="about-text">

        ...

    </div>

</div>
```

Получается следующая структура:

```text
about-container
│
├── about-photo
│      └── img
│
└── about-text
       ├── p
       └── p
```

---

# Тег `<img>`

Впервые в нашем проекте появился тег

```html
<img>
```

Он используется для отображения изображений.

Внутри него есть два важных атрибута.

```html
src="assets/img/me.jpg"
```

`src` показывает путь к изображению.

Именно здесь браузер ищет фотографию.

---

## Атрибут `alt`

```html
alt="Моя фотография"
```

`alt` — альтернативное описание изображения.

Если фотография не загрузится, пользователь увидит этот текст.

Кроме того, `alt` помогает людям, которые используют программы для чтения текста с экрана.

---

# Контейнер `.about-container`

Теперь посмотрим на CSS.

```css
.about-container{

    display:flex;

    align-items:center;

    gap:50px;

    margin-top:40px;

    flex-wrap:wrap;

}
```

Мы снова используем Flexbox.

Теперь он помогает расположить фотографию и текст рядом друг с другом.

---

# Свойство `flex-wrap`

Сегодня появилось новое свойство.

```css
flex-wrap:wrap;
```

Если места на экране станет мало (например, на телефоне), элементы автоматически перейдут на новую строку.

Это делает сайт более удобным на разных устройствах.

---

# Работа с изображением

Следующее правило относится к фотографии.

```css
.about-photo img{

    width:300px;

    height:300px;

    object-fit:cover;

    border-radius:20px;

    border:3px solid #58a6ff;

}
```

---

# Размер изображения

Мы задали фотографии одинаковую ширину и высоту.

```css
width:300px;

height:300px;
```

Теперь изображение всегда имеет одинаковый размер.

---

# Свойство `object-fit`

Новое свойство

```css
object-fit:cover;
```

помогает красиво вписать изображение в заданные размеры.

Фотография не растягивается и не сжимается.

Лишние части изображения аккуратно обрезаются.

Именно поэтому фотография выглядит естественно.

---

# Свойство `border`

```css
border:3px solid #58a6ff;
```

Добавляет рамку вокруг изображения.

Она состоит из трех частей:

* толщина — `3px`;
* стиль — `solid`;
* цвет — `#58a6ff`.

---

# Свойство `border-radius`

```css
border-radius:20px;
```

Закругляет углы фотографии.

Теперь изображение выглядит современно и аккуратно.

---

# Блок с текстом

Следующее правило:

```css
.about-text{

    flex:1;

    min-width:300px;

}
```

---

# Свойство `flex`

Сегодня мы впервые познакомились с записью

```css
flex:1;
```

Она говорит браузеру:

> "Этот блок может занимать всё свободное место."

Поэтому текст красиво располагается рядом с фотографией.

---

# Свойство `min-width`

```css
min-width:300px;
```

Определяет минимальную ширину блока.

Если экран станет слишком маленьким, текст не будет сжиматься до неудобного размера.

Это делает сайт более удобным на мобильных устройствах.

---

# Отступ между абзацами

```css
.about-text p{

    line-height:1.8;

    margin-bottom:20px;

}
```

Мы уже знакомы с обоими свойствами.

* `line-height` делает текст более читаемым.
* `margin-bottom` добавляет расстояние между абзацами.

---

# Оформление Footer

Последним элементом страницы стал footer.

```css
footer{

    padding:50px;

    text-align:center;

    background:#161b22;

}
```

Мы уже знакомы почти со всеми этими свойствами.

Новым стало только

```css
text-align:center;
```

Оно выравнивает текст по центру.

Теперь подпись внизу страницы выглядит аккуратно.

---

# Что получилось?

После сегодняшнего урока сайт стал практически завершенным.

Теперь:

* появилась фотография;
* текст располагается рядом с изображением;
* фотография получила рамку и закругленные углы;
* сайт стал удобнее на небольших экранах;
* footer приобрел законченный вид.

---

# Итоги урока

После восьмого занятия мы научились:

* добавлять изображения на страницу;
* использовать атрибуты `src` и `alt`;
* работать со свойством `object-fit`;
* создавать рамки вокруг элементов;
* использовать `flex-wrap`;
* использовать `flex`;
* задавать минимальную ширину элементов;
* оформлять нижнюю часть сайта.

Теперь наш сайт-визитка полностью оформлен и готов к переходу на следующий этап обучения.


# Урок №9. Подключение внешней библиотеки и добавление иконок

## Цель урока

На этом занятии мы сделали сайт более современным.

Мы познакомились с понятием **внешней библиотеки**, подключили библиотеку **Font Awesome** и заменили обычный текст ссылок красивыми иконками социальных сетей.

После этого урока сайт стал выглядеть намного профессиональнее.

---

# Повторение

На предыдущих занятиях мы:

* создали структуру сайта;
* оформили страницу с помощью CSS;
* добавили фотографию;
* завершили оформление всех разделов.

Сегодня мы познакомились с еще одним важным инструментом веб-разработчика.

---

# Что такое библиотека?

Во время разработки сайтов программисты редко создают абсолютно всё самостоятельно.

Очень часто используются готовые библиотеки.

**Библиотека** — это набор уже готовых инструментов, которые можно подключить к своему проекту.

Например:

* красивые иконки;
* готовые кнопки;
* анимации;
* шрифты;
* различные компоненты интерфейса.

Использование библиотек позволяет значительно ускорить разработку.

---

# Что такое Font Awesome?

**Font Awesome** — одна из самых популярных библиотек иконок.

В ней собраны тысячи готовых изображений:

* GitHub;
* Telegram;
* VK;
* Email;
* YouTube;
* Discord;
* Python;
* Docker;

и многие другие.

Разработчику достаточно подключить библиотеку и выбрать нужную иконку.

---

# Что такое CDN?

Мы подключили библиотеку не скачивая ее на компьютер.

Для этого использовали **CDN**.

CDN (Content Delivery Network) — это сеть серверов, на которых уже хранятся готовые файлы.

Вместо загрузки библиотеки в проект браузер получает ее через Интернет.

Это очень удобно, потому что:

* не нужно хранить файлы в проекте;
* подключение занимает всего одну строку;
* библиотека всегда доступна по ссылке.

---

# Подключение Font Awesome

В файл `index.html` мы добавили строку:

```html id="0l8mrv"
<link rel="stylesheet"
href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css">
```

Она сообщает браузеру:

> "Загрузи стили библиотеки Font Awesome."

После этого становятся доступны все бесплатные иконки.

---

# Изменение HTML

Раньше ссылки выглядели так:

```html id="6rmfxg"
<a href="...">

    GitHub

</a>
```

Теперь внутри ссылки появился новый тег.

```html id="0wj3u4"
<a href="...">

    <i class="fa-brands fa-github"></i>

    GitHub

</a>
```

---

# Тег `<i>`

Для отображения иконки используется тег

```html id="kjl2d4"
<i>
```

Сам по себе он ничего не рисует.

Главную роль здесь играет атрибут

```html id="8i3r9o"
class=""
```

Именно классы говорят библиотеке, какую иконку нужно показать.

Например:

```html id="ewt2qe"
fa-brands fa-github
```

или

```html id="tdadtx"
fa-brands fa-telegram
```

---

# Какие иконки мы использовали?

В проект были добавлены:

* GitHub;
* Telegram;
* VK;
* Email.

Теперь пользователь сразу понимает, куда ведет каждая ссылка.

---

# Изменение стилей

Мы обновили оформление ссылок.

```css id="m3qppx"
.socials a{

    display:flex;

    align-items:center;

    gap:10px;

    text-decoration:none;

    padding:15px 30px;

    background:#238636;

    color:white;

    border-radius:8px;

    transition:.3s;

}
```

Большинство свойств мы уже изучали ранее.

Появились лишь небольшие изменения.

---

# Свойство `gap`

```css id="fj6rkz"
gap:10px;
```

Теперь расстояние создается не между кнопками, а между иконкой и текстом.

Благодаря этому ссылка выглядит аккуратно.

---

# Почему снова используется Flexbox?

```css id="twrz91"
display:flex;

align-items:center;
```

Flexbox помогает расположить:

* иконку;
* текст.

на одной линии.

Именно поэтому подписи выглядят красиво.

---

# Анимация кнопок

Мы добавили новый эффект.

```css id="5jmljlwm"
.socials a:hover{

    transform:translateY(-3px);

    background:#2ea043;

}
```

Теперь при наведении мыши:

* кнопка немного поднимается вверх;
* изменяет цвет.

Мы уже использовали похожий эффект для главной кнопки и карточек.

Теперь сайт выглядит более "живым".

---

# Почему иконки белые?

Мы не задавали цвет иконкам отдельно.

Они автоматически получили цвет текста.

Потому что внутри ссылки используется свойство

```css id="g7j25k"
color:white;
```

Это хороший пример наследования стилей в CSS.

---

# Современный вариант оформления

Мы также познакомились с альтернативным вариантом оформления контактов.

Вместо длинных кнопок можно использовать только круглые иконки.

Например:

```text id="m0h9n8"
○ GitHub

○ Telegram

○ VK

○ Email
```

Для этого достаточно изменить несколько свойств.

```css id="lmr5f2"
.socials a{

    width:55px;

    height:55px;

    display:flex;

    justify-content:center;

    align-items:center;

    border-radius:50%;

    font-size:24px;

}
```

Теперь ссылки превращаются в круглые кнопки.

Такое оформление часто встречается на современных сайтах и в портфолио разработчиков.

---

# Что получилось?

После сегодняшнего урока сайт стал выглядеть значительно современнее.

Теперь:

* ссылки получили узнаваемые иконки;
* контакты стали удобнее восприниматься;
* кнопки получили красивую анимацию;
* мы познакомились с первой внешней библиотекой.

---

# Итоги урока

После девятого занятия мы научились:

* понимать, что такое библиотека;
* познакомились с Font Awesome;
* узнали, что такое CDN;
* подключать внешние библиотеки к проекту;
* использовать готовые иконки;
* размещать иконки рядом с текстом;
* создавать современные кнопки социальных сетей;
* познакомились с наследованием цветов в CSS.

После этого урока оформление нашего сайта практически завершено. На следующих занятиях мы начнем изучать JavaScript и сделаем страницу не только красивой, но и интерактивной.


# Урок №10. Первое знакомство с JavaScript и подключение готового функционала

## Цель урока

На этом занятии мы впервые познакомились с JavaScript.

Мы не изучали сам язык программирования, а разобрали его роль в создании сайта.

Мы узнали, почему одного HTML и CSS недостаточно и зачем нужен JavaScript.

---

# Повторение

На предыдущих уроках мы создали:

* структуру проекта;
* HTML-разметку;
* красивый дизайн с помощью CSS;
* подключили внешние библиотеки.

Сейчас наш сайт выглядит хорошо, но он остается практически статичным.

---

# Три главные технологии сайта

Современный сайт обычно состоит из трех основных частей:

```text
HTML
│
├── создает структуру
│
CSS
│
├── отвечает за внешний вид
│
JavaScript
│
└── добавляет действия
```

---

# Аналогия с домом

Можно представить сайт как дом.

HTML:

> Это стены, комнаты и двери.

CSS:

> Это ремонт, цвет стен и мебель.

JavaScript:

> Это электричество, свет и автоматические двери.

---

# Зачем нужен JavaScript?

JavaScript позволяет сайту реагировать на действия пользователя.

Например:

* нажали кнопку → что-то произошло;
* открыли меню → появилось окно;
* нажали Play → включилась музыка;
* отправили форму → данные отправились.

---

# Новый файл script.js

В структуре проекта появился файл:

```
assets/

└── js/

    └── script.js
```

Он предназначен для JavaScript-кода.

---

# Почему появился отдельный player.js?

Мы создали отдельный файл:

```
assets/

└── js/

    └── player.js
```

Это хорошая практика.

Когда проект становится больше, код разделяют по назначению.

Например:

```text
script.js

общие действия сайта


player.js

логика музыкального плеера
```

Так легче искать ошибки и изменять проект.

---

# Подключение JavaScript

Чтобы браузер увидел JavaScript, его нужно подключить.

В HTML добавляется:

```html
<script src="assets/js/script.js"></script>

<script src="assets/js/player.js"></script>
```

Теперь браузер знает, где находятся дополнительные инструкции.

---

# Почему порядок подключения важен?

Браузер читает код сверху вниз.

Поэтому порядок файлов может иметь значение.

Например:

```html
script.js

player.js
```

означает:

сначала загрузить первый файл,

потом второй.

---

# Что важно запомнить

Сегодня мы не изучали JavaScript.

Наша цель была понять:

* зачем он нужен;
* где находится в проекте;
* как подключается;
* почему файлы разделяются.

К самому программированию на JavaScript мы вернемся позже.

---

# Итоги урока

После десятого занятия мы научились:

* понимать роль JavaScript;
* отличать HTML, CSS и JS;
* создавать папку js;
* подключать JavaScript-файлы;
* понимать назначение нескольких JS-файлов;
* разделять код проекта по назначению.

Теперь наш проект имеет полноценную структуру настоящего веб-приложения.


# Урок №11. Создание структуры аудиоплеера (HTML)

## Цель урока

На этом занятии мы добавили на сайт новый интерактивный элемент — музыкальный плеер.

Мы не изучали JavaScript, а разобрали, как с помощью HTML создать структуру сложного блока.

Мы узнали, как:

* создавать большие компоненты из маленьких частей;
* использовать классы для CSS;
* использовать `id` для связи с JavaScript;
* подключать готовые иконки;
* создавать структуру будущего функционала.

---

# Что такое компонент сайта?

До этого момента наш сайт состоял из больших разделов:

* header;
* hero;
* about;
* skills;
* contacts;
* footer.

Теперь мы добавили новый компонент:

```text id="0x7hzf"
Аудиоплеер
```

Компонент — это отдельная часть сайта, которая выполняет свою задачу.

Например:

* меню;
* карточка товара;
* форма обратной связи;
* плеер.

---

# Общая структура плеера

Наш плеер состоит из нескольких частей:

```text id="n4pqk7"
terminal-player

│

├── terminal-header

│      ├── кнопки окна

│      ├── название

│      └── стрелка

│

└── terminal-body

       ├── статус

       ├── название трека

       ├── кнопки управления

       ├── эквалайзер

       └── громкость
```

Так разработчики обычно проектируют сложные элементы.

Сначала создают структуру, потом оформляют дизайн, потом добавляют логику.

---

# Главный контейнер плеера

Начинается плеер с:

```html
<div class="terminal-player collapsed" id="terminalPlayer">
```

Здесь мы видим два важных понятия:

* `class`;
* `id`.

---

# Что такое class?

```html
class="terminal-player"
```

Класс нужен для CSS.

Например:

```css
.terminal-player{

}
```

Браузер понимает:

> "Применить эти стили ко всем элементам с таким классом."

Классы можно использовать много раз.

---

# Что такое id?

```html
id="terminalPlayer"
```

`id` — это уникальное имя элемента.

Обычно он используется для JavaScript.

Например:

JavaScript сможет сказать:

> "Найди элемент terminalPlayer и измени его."

В отличие от класса, один `id` должен принадлежать одному элементу.

---

# Почему используются и class, и id?

Потому что они выполняют разные задачи.

Например:

```html
<div 
class="terminal-player"
id="terminalPlayer">
</div>
```

Здесь:

```text
class
↓
отвечает за внешний вид


id
↓
отвечает за управление
```

Это очень распространенный подход в веб-разработке.

---

# Шапка плеера

Следующий блок:

```html
<div class="terminal-header" id="togglePlayer">
```

Это верхняя часть плеера.

Она содержит:

* кнопки окна;
* название;
* стрелку открытия.

---

# Кнопки терминала

```html
<span class="terminal-dot red"></span>

<span class="terminal-dot yellow"></span>

<span class="terminal-dot green"></span>
```

Это декоративные элементы.

Они имитируют кнопки окна терминала.

Каждый элемент имеет два класса:

Например:

```html
class="terminal-dot red"
```

Здесь:

```text
terminal-dot
```

общий стиль кнопки

```text
red
```

цвет конкретной кнопки.

---

# Использование Font Awesome

Для стрелки используется:

```html
<i class="fa-solid fa-chevron-up"></i>
```

Мы уже знакомы с этим подходом.

Библиотека Font Awesome позволяет использовать готовые иконки.

---

# Тело плеера

Основная часть:

```html
<div class="terminal-body">
```

Здесь располагается вся информация.

---

# Строки терминала

Например:

```html
<div class="terminal-line">

    <span class="terminal-command">
        $
    </span>

    status

</div>
```

Мы сделали стиль терминала.

Символ:

```text
$
```

имитирует командную строку Linux.

Например:

```text
$ status
```

выглядит как команда в терминале.

---

# Статус проигрывания

```html
<div class="terminal-status"
id="playerStatus">

    paused

</div>
```

Здесь будет отображаться состояние плеера.

Например:

```text
paused
```

пауза.

Позже JavaScript сможет менять его:

```text
playing
```

воспроизведение.

---

# Название трека

```html
<div class="track-title"
id="trackTitle">

    No track loaded

</div>
```

Этот блок показывает текущую композицию.

Сейчас написано:

```text
No track loaded
```

Позже JavaScript сможет заменить этот текст.

---

# Кнопки управления

```html
<div class="player-controls">
```

Внутри находятся:

* предыдущий трек;
* запуск/пауза;
* следующий трек.

Пример:

```html
<button id="playPause">

    <i class="fa-solid fa-play"></i>

</button>
```

---

# Почему кнопки имеют id?

Например:

```html
id="playPause"
```

JavaScript сможет найти именно эту кнопку.

То есть:

HTML говорит:

> "Вот кнопка."

CSS говорит:

> "Вот как она выглядит."

JavaScript говорит:

> "Вот что она делает."

---

# Эквалайзер

```html
<div class="equalizer">
```

Внутри:

```html
<span></span>
<span></span>
<span></span>
```

Это заготовка для визуального эффекта.

Позже можно сделать движение полосок во время воспроизведения.

---

# Громкость

Последний блок:

```html
<input
type="range">
```

Это стандартный HTML-элемент.

Он создает ползунок.

Параметры:

```html
min="0"
max="1"
step="0.01"
```

означают:

минимальное значение:

```text
0
```

максимальное:

```text
1
```

шаг изменения:

```text
0.01
```

---

# Подключение JavaScript

В конце страницы появились:

```html
<script src="assets/js/script.js"></script>

<script src="assets/js/player.js"></script>
```

Теперь браузер знает:

где находятся дополнительные инструкции.

---

# Почему JS подключается в конце страницы?

Мы разместили скрипты перед:

```html
</body>
```

Это хорошая практика.

Сначала браузер загружает HTML.

Создает все элементы.

И только потом запускает JavaScript.

Так меньше вероятность ошибок.

---

# Что получилось?

После сегодняшнего урока на сайте появился полноценный новый компонент.

Мы создали:

* окно плеера;
* кнопки управления;
* отображение статуса;
* название трека;
* управление громкостью;
* основу для будущей логики.

---

# Итоги урока

После одиннадцатого занятия мы научились:

* создавать сложные HTML-компоненты;
* понимать структуру больших блоков;
* использовать классы и id;
* понимать разницу между class и id;
* подключать готовые иконки;
* создавать основу интерактивного элемента;
* подключать JavaScript-файлы.

На следующем уроке мы займемся оформлением плеера с помощью CSS.


# Урок №12. Создание дизайна музыкального плеера

## Цель урока

На этом занятии мы оформили музыкальный плеер.

До этого момента он представлял собой обычный набор HTML-элементов.

Теперь с помощью CSS мы превратили его в современный интерфейс, напоминающий окно терминала.

Во время работы мы повторили уже знакомые свойства CSS и познакомились с несколькими новыми.

---

# Главный контейнер

Первым оформляется контейнер

```css
.terminal-player{
```

Именно он становится основой всего плеера.

Здесь обычно задаются:

* размеры;
* фон;
* положение на странице;
* скругление углов;
* тени.

Можно сказать, что это "корпус" нашего плеера.

---

# Скругление углов

Мы снова использовали

```css
border-radius
```

Теперь этот эффект применяется уже не к кнопке, а ко всему окну.

Интерфейс становится мягче и современнее.

---

# Тень

Новым свойством стало

```css
box-shadow
```

Оно добавляет тень вокруг элемента.

Без тени блок выглядит плоским.

После добавления тени создается ощущение, что окно немного приподнято над страницей.

Именно этот прием используют большинство современных сайтов.

---

# Позиционирование

Еще одно новое свойство —

```css
position
```

Оно определяет, где именно будет находиться элемент.

Например, плеер можно закрепить:

* внизу страницы;
* сверху;
* справа;
* слева.

Благодаря этому пользователь всегда может быстро открыть музыку.

---

# Цвета и градиенты

Для оформления плеера можно использовать:

```css
background
```

или

```css
background: linear-gradient(...)
```

Градиент — это плавный переход одного цвета в другой.

Он делает интерфейс значительно красивее.

Мы познакомились с новым способом оформления фона.

---

# Заголовок терминала

Следующим оформляется

```css
.terminal-header
```

Это верхняя часть плеера.

Мы снова использовали Flexbox.

Теперь:

* цветные кнопки находятся слева;
* название — по центру;
* стрелка открытия — справа.

Flexbox снова помог расположить элементы аккуратно.

---

# Кнопки терминала

Три кружка:

```text
🔴 🟡 🟢
```

создаются обычными элементами

```html
<span>
```

Но благодаря CSS превращаются в маленькие цветные кнопки.

Мы повторили свойства:

* width;
* height;
* background;
* border-radius.

Оказывается, даже простой `<span>` можно превратить в красивый элемент интерфейса.

---

# Заголовок плеера

Название

```text
Relax time 🎧
```

оформляется так же, как обычный текст.

Мы повторили:

* font-size;
* color;
* font-weight;
* text-align.

---

# Строки терминала

Команды

```text
$ status

$ now_playing
```

оформляются с помощью разных цветов.

Так создается ощущение настоящей командной строки Linux.

Для этого используются уже знакомые свойства:

* color;
* margin;
* font-family.

---

# Кнопки управления

Следующим оформляется блок

```css
.player-controls
```

Мы снова используем Flexbox.

Теперь кнопки:

* располагаются в одну строку;
* имеют одинаковый размер;
* красиво выровнены по центру.

---

# Круглые кнопки

Кнопки получают:

```css
width

height

border-radius:50%;
```

Последнее свойство делает квадрат полностью круглым.

Так создаются современные кнопки воспроизведения музыки.

---

# Эффект наведения

Мы снова использовали

```css
:hover
```

и

```css
transition
```

Теперь кнопки плавно меняют цвет и слегка увеличиваются или поднимаются.

Мы уже использовали этот прием:

* в меню;
* на главной кнопке;
* на карточках;
* в контактах.

Получается, что один и тот же инструмент можно применять ко многим элементам сайта.

---

# Эквалайзер

Следующим оформляется блок

```css
.equalizer
```

Несколько маленьких

```html
<span>
```

превращаются в полоски эквалайзера.

Каждая полоска получает:

* ширину;
* высоту;
* цвет;
* скругление.

Позже JavaScript сможет изменять их высоту во время воспроизведения музыки.

---

# Ползунок громкости

Последним оформляется

```html
<input type="range">
```

Это стандартный элемент HTML.

С помощью CSS можно изменить его внешний вид.

Теперь обычный системный ползунок становится частью дизайна нашего сайта.

---

# Почему CSS оказался таким важным?

В начале урока наш плеер представлял собой обычный набор текста и кнопок.

После добавления стилей он стал похож на настоящее приложение.

Это еще раз показывает главную задачу CSS:

> HTML создает структуру.

> CSS превращает структуру в красивый интерфейс.

---

# Что получилось?

После сегодняшнего урока сайт получил полноценный музыкальный плеер.

Теперь он:

* выглядит как окно терминала;
* имеет современный дизайн;
* содержит красивые кнопки;
* использует Flexbox;
* поддерживает эффекты наведения;
* имеет оформленный ползунок громкости;
* полностью готов к работе JavaScript.

---

# Итоги урока

После двенадцатого занятия мы:

* оформили музыкальный плеер;
* познакомились с новыми дизайнерскими приемами CSS;
* научились использовать `box-shadow`;
* познакомились с градиентами;
* повторили Flexbox;
* научились оформлять стандартные HTML-элементы;
* создали современный интерфейс музыкального проигрывателя.


# Урок №12. Оформление музыкального плеера. Часть 1

## Цель урока

На этом занятии мы продолжили оформление музыкального плеера.

Мы создали внешний вид окна терминала, оформили его заголовок, кнопки управления и познакомились с несколькими новыми свойствами CSS.

После этого урока плеер стал похож на настоящее приложение.

---

# Повторение

На прошлом занятии мы создали структуру плеера с помощью HTML.

Теперь пришло время превратить обычные `<div>` в красивый интерфейс.

---

# Главный контейнер плеера

Все оформление начинается с главного контейнера.

```css
.terminal-player{

    ...

}
```

Именно он объединяет все остальные элементы.

---

# Свойство `position`

Первое новое свойство:

```css
position:fixed;
```

Оно закрепляет элемент на экране.

Это означает, что даже если пользователь прокручивает страницу вниз, плеер остается на своем месте.

Такой прием часто используют для:

* музыкальных плееров;
* кнопок "Наверх";
* онлайн-чатов;
* уведомлений.

---

# Свойства `right` и `bottom`

Следующие строки:

```css
right:30px;

bottom:30px;
```

Определяют положение элемента.

Плеер располагается:

* на расстоянии **30 пикселей справа**;
* на расстоянии **30 пикселей снизу**.

Получается удобное расположение в правом нижнем углу страницы.

---

# Свойство `width`

```css
width:340px;
```

Задает ширину окна плеера.

Теперь независимо от содержимого окно остается одинакового размера.

---

# Цвет фона

```css
background:#161b22;
```

Мы уже знакомы с этим свойством.

Фон выбран в темных оттенках, чтобы напоминать окно терминала.

---

# Свойство `border`

```css
border:1px solid rgba(255,255,255,.1);
```

Создает тонкую рамку вокруг окна.

Рамка состоит из трех частей:

* толщина — `1px`;
* стиль — `solid`;
* цвет — `rgba(...)`.

---

# Что такое `rgba()`?

Раньше мы использовали цвета вида:

```css
#58a6ff
```

Теперь появился новый способ записи цвета.

```css
rgba(255,255,255,.1)
```

Первые три числа отвечают за цвет:

```text
255 255 255
```

Это белый цвет.

Последнее значение

```text
0.1
```

отвечает за прозрачность.

Получается очень легкая полупрозрачная рамка.

---

# Скругление углов

```css
border-radius:12px;
```

Мы уже использовали это свойство.

Теперь закругляются углы всего окна плеера.

---

# Свойство `overflow`

Новое свойство:

```css
overflow:hidden;
```

Оно говорит браузеру:

> "Если содержимое выходит за границы блока — скрыть его."

Это особенно важно после добавления закругленных углов.

Без этого свойства некоторые элементы могли бы выходить за пределы окна.

---

# Свойство `box-shadow`

```css
box-shadow:0 15px 40px rgba(0,0,0,.45);
```

Добавляет тень.

Тень делает окно более объемным.

Создается ощущение, что плеер немного приподнят над страницей.

Такой прием очень часто используется в современном дизайне.

---

# Свойство `z-index`

Еще одно новое свойство:

```css
z-index:999;
```

Оно определяет, какой элемент находится "выше" остальных.

Большое значение гарантирует, что плеер не окажется под другими элементами страницы.

---

# Плавные изменения

```css
transition:.35s ease;
```

Мы уже знакомы со свойством `transition`.

Теперь дополнительно появилось слово

```css
ease
```

Оно делает начало и конец анимации более плавными.

---

# Свойство `font-family`

```css
font-family:

"JetBrains Mono",

"Consolas",

monospace;
```

Теперь плеер использует специальный моноширинный шрифт.

Такие шрифты часто используются в:

* терминалах;
* редакторах кода;
* консоли.

Именно поэтому окно начинает напоминать настоящий терминал.

---

# Сворачивание плеера

Следующее правило:

```css
.terminal-player.collapsed{

    height:48px;

}
```

Если у элемента появляется класс

```text
collapsed
```

его высота уменьшается.

Остается видимой только верхняя панель.

---

# Открытый плеер

```css
.terminal-player:not(.collapsed){

    height:auto;

}
```

Здесь используется новый селектор.

```css
:not(...)
```

Он означает:

> "Выбрать элементы, которые НЕ имеют указанный класс."

Если класс `collapsed` отсутствует, высота автоматически подстраивается под содержимое.

---

# Верхняя панель

Следующий блок:

```css
.terminal-header{

    ...

}
```

Это верхняя часть плеера.

Здесь располагаются:

* цветные кнопки;
* название;
* стрелка.

---

# Повторение Flexbox

Мы снова используем

```css
display:flex;
```

и уже знакомые свойства:

```css
align-items:center;

justify-content:space-between;
```

Благодаря этому все элементы располагаются аккуратно в одну строку.

---

# Свойство `cursor`

Новое свойство:

```css
cursor:pointer;
```

Когда пользователь наводит мышь на заголовок, курсор превращается в руку.

Это подсказывает:

> "На этот элемент можно нажать."

---

# Кнопки терминала

Контейнер:

```css
.terminal-buttons
```

снова использует Flexbox.

Расстояние между кружками задается:

```css
gap:8px;
```

---

# Цветные кружки

Каждая кнопка получает:

```css
width:12px;

height:12px;

border-radius:50%;
```

Квадрат превращается в круг.

После этого разные классы задают цвет:

```css
.red
```

красный,

```css
.yellow
```

желтый,

```css
.green
```

зеленый.

Так мы повторяем принцип:

* один класс отвечает за форму;
* второй — за цвет.

---

# Заголовок плеера

```css
.terminal-title
```

Использует уже знакомые свойства:

* `color`;
* `font-size`.

Новым стало:

```css
letter-spacing:1px;
```

Это свойство увеличивает расстояние между буквами.

Текст становится более похожим на оформление терминала.

---

# Стрелка открытия

```css
#terminalArrow
```

Получает цвет и плавную анимацию.

Когда плеер сворачивается:

```css
transform:rotate(180deg);
```

стрелка поворачивается.

Пользователь сразу понимает текущее состояние плеера.

---

# Основная часть

```css
.terminal-body{

    padding:20px;

}
```

Добавляет внутренние отступы.

Теперь содержимое не касается краев окна.

---

# Строки терминала

```css
.terminal-line
```

Мы изменяем:

* цвет;
* размер шрифта;
* нижний отступ.

Отдельно оформляется символ

```text
$
```

через класс

```css
.terminal-command
```

Зеленый цвет делает терминал более реалистичным.

---

# Статус плеера

```css
.terminal-status
```

Показывает состояние проигрывателя.

Например:

```text
paused
```

или

```text
playing
```

Мы изменили:

* цвет;
* размер текста;
* нижний отступ.

---

# Название композиции

```css
.track-title
```

Здесь отображается название текущего трека.

Новым свойством стало:

```css
min-height:20px;
```

Оно задает минимальную высоту блока.

Даже если название пока отсутствует, размер плеера остается одинаковым.

---

# Кнопки управления

Контейнер:

```css
.player-controls
```

использует Flexbox.

Кнопки располагаются:

* по центру;
* на одинаковом расстоянии друг от друга.

---

# Оформление кнопок

Каждая кнопка получает:

* одинаковую ширину;
* одинаковую высоту;
* круглую форму;
* цвет;
* плавную анимацию.

Мы повторили свойства:

* `width`;
* `height`;
* `border-radius`;
* `display:flex`;
* `justify-content`;
* `align-items`;
* `transition`.

---

# Наведение мыши

При наведении:

```css
:hover
```

кнопка:

* становится светлее;
* немного поднимается вверх.

Так пользователь сразу видит, что элемент активный.

---

# Центральная кнопка

Последнее правило:

```css
.player-controls button:nth-child(2)
```

выбирает вторую кнопку внутри блока.

Именно поэтому кнопка воспроизведения получилась:

* больше остальных;
* другого цвета.

Она сразу привлекает внимание пользователя.

---

# Что получилось?

После сегодняшнего урока музыкальный плеер приобрел современный внешний вид.

Теперь он:

* закреплен в правом нижнем углу страницы;
* напоминает окно терминала;
* имеет красивую тень;
* поддерживает сворачивание;
* содержит стильные кнопки управления.

---

# Итоги урока

После двенадцатого занятия мы научились:

* использовать `position: fixed`;
* располагать элементы с помощью `right` и `bottom`;
* познакомились с цветами `rgba`;
* использовать `overflow`;
* создавать тени с помощью `box-shadow`;
* работать со свойством `z-index`;
* познакомились с селектором `:not()`;
* использовать `cursor`;
* изменять расстояние между буквами с помощью `letter-spacing`;
* выбирать элементы через `:nth-child()`;
* повторили Flexbox и уже знакомые свойства CSS.

На следующем уроке мы завершим оформление музыкального плеера, добавим эквалайзер, оформим ползунок громкости и подготовим сайт к работе готового JavaScript-кода.


# Урок №13. Завершение оформления музыкального плеера. Анимация и адаптивность

## Цель урока

На этом занятии мы завершили оформление музыкального плеера.

Мы познакомились с CSS-анимацией, оформили эквалайзер и регулятор громкости, а также впервые сделали сайт удобным для просмотра на разных устройствах.

После этого урока оформление сайта было полностью завершено.

---

# Повторение

На прошлом занятии мы оформили:

* окно плеера;
* заголовок;
* кнопки управления;
* название композиции;
* статус проигрывателя.

Сегодня осталось закончить последние элементы.

---

# Эквалайзер

Начнем с блока

```css
.equalizer{
```

Эквалайзер — это небольшие полоски, которые двигаются во время воспроизведения музыки.

Даже если они не влияют на звук, они делают плеер более живым и современным.

---

# Flexbox снова помогает

```css
display:flex;
```

Мы уже много раз использовали Flexbox.

Теперь он помогает расположить полоски эквалайзера рядом друг с другом.

---

# Свойство `align-items`

```css
align-items:flex-end;
```

Раньше мы использовали

```css
align-items:center;
```

Теперь появилось новое значение

```css
flex-end
```

Оно прижимает все полоски к нижней части контейнера.

Именно поэтому они растут вверх, а не вниз.

---

# Свойство `gap`

```css
gap:5px;
```

Добавляет одинаковое расстояние между полосками.

Благодаря этому эквалайзер выглядит аккуратно.

---

# Полоски эквалайзера

Каждая полоска оформляется отдельно.

```css
.equalizer span{
```

Мы задаем:

* ширину;
* начальную высоту;
* цвет;
* скругление углов.

Несколько обычных `<span>` превращаются в настоящий эквалайзер.

---

# Первое знакомство с CSS-анимацией

Сегодня мы изучили совершенно новую возможность CSS.

```css
animation
```

До этого момента элементы могли только менять внешний вид при наведении мыши.

Теперь они могут двигаться самостоятельно.

---

# Свойство `animation`

Например:

```css
animation: equalize .8s infinite alternate;
```

Разберем его по частям.

---

## Имя анимации

```css
equalize
```

Это название нашей анимации.

Именно оно связывает элемент с блоком

```css
@keyframes equalize
```

---

## Длительность

```css
.8s
```

Анимация длится 0.8 секунды.

После этого начинается заново.

---

## Значение `infinite`

```css
infinite
```

Означает:

анимация никогда не остановится.

Она будет повторяться бесконечно.

---

## Значение `alternate`

```css
alternate
```

После завершения анимации элемент движется обратно.

Получается плавное движение вверх и вниз.

---

# Почему полоски двигаются по-разному?

Каждая полоска использует разную скорость.

Например:

```css
:nth-child(1)
```

работает 0.8 секунды,

а

```css
:nth-child(2)
```

— уже 0.5 секунды.

Благодаря этому движение выглядит естественным.

Если бы все полоски двигались одинаково, эквалайзер выглядел бы искусственно.

---

# Что такое `@keyframes`?

Это правило описывает саму анимацию.

```css
@keyframes equalize
```

Внутри мы указываем:

откуда начинается движение

и

где оно заканчивается.

---

# Блок `from`

```css
from{

    height:8px;

}
```

В начале анимации полоска имеет небольшую высоту.

---

# Блок `to`

```css
to{

    height:32px;

}
```

В конце анимации полоска становится выше.

После этого благодаря

```css
alternate
```

она снова уменьшается.

Так появляется эффект работы музыкального эквалайзера.

---

# Регулятор громкости

Следующий блок

```css
.player-volume{
```

оформляет нижнюю часть плеера.

Здесь располагаются:

* иконка громкости;
* ползунок.

---

# Flexbox

Мы снова используем

```css
display:flex;
```

чтобы элементы располагались в одну строку.

---

# Свойство `flex`

Для ползунка используется

```css
flex:1;
```

Мы уже встречали это свойство раньше.

Теперь оно позволяет ползунку занять все свободное место.

---

# Курсор

```css
cursor:pointer;
```

Пользователь сразу понимает, что ползунок можно передвигать.

---

# Что такое адаптивная верстка?

До этого момента сайт хорошо выглядел только на большом экране.

Но современные сайты открываются на:

* компьютерах;
* ноутбуках;
* планшетах;
* телефонах.

Поэтому сайт должен автоматически менять свой внешний вид.

Это называется **адаптивной версткой**.

---

# Первое знакомство с `@media`

Мы использовали новое правило

```css
@media
```

Оно позволяет применять разные стили для разных размеров экрана.

---

# Первый адаптивный режим

```css
@media (max-width:900px)
```

Это означает:

если ширина экрана меньше 900 пикселей —

использовать другие стили.

Например:

* уменьшается фотография;
* изменяются отступы;
* плеер становится шире;
* главный экран перестраивается.

---

# Второй адаптивный режим

```css
@media (max-width:600px)
```

Это уже телефоны.

Здесь происходит еще больше изменений.

---

# Что меняется на телефоне?

Мы изменяем:

* размер логотипа;
* внутренние отступы;
* размеры заголовков;
* расположение социальных сетей.

Кроме того,

```css
nav{

    display:none;

}
```

скрывает верхнее меню.

На маленьком экране оно занимает слишком много места.

На настоящих сайтах вместо него обычно появляется кнопка "бургер-меню".

---

# Почему это важно?

Без адаптивности сайт мог бы выглядеть хорошо только на компьютере.

На телефоне пришлось бы постоянно увеличивать страницу и прокручивать ее в стороны.

Сегодня мы сделали сайт удобным практически для любого устройства.

---

# Что получилось?

После сегодняшнего урока мы полностью завершили оформление сайта.

Теперь он:

* содержит современный музыкальный плеер;
* имеет анимированный эквалайзер;
* поддерживает адаптивную верстку;
* корректно отображается на больших и маленьких экранах.

---

# Итоги урока

После тринадцатого занятия мы научились:

* создавать CSS-анимации;
* использовать свойство `animation`;
* работать с правилом `@keyframes`;
* использовать значения `infinite` и `alternate`;
* познакомились с адаптивной версткой;
* использовать правило `@media`;
* изменять внешний вид сайта для разных размеров экрана;
* завершили оформление нашего проекта.

Теперь сайт полностью готов к подключению готовой JavaScript-логики, которая сделает музыкальный плеер интерактивным.

